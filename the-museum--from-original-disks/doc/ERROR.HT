/* FILE = error.h */

/* Definitions of externally used functions.
 */

#ifdef MSDOS
extern  void    error(char *);
#else
extern  void    error();
#endif

#ifdef MSDOS
extern  void    error_f(char *, char *);
#else
extern  void    error_f();
#endif

#ifdef MSDOS
extern  void    error_m(char *, char *);
#else
extern  void    error_m();
#endif

#ifdef MSDOS
extern  int     interrupt_handler(void);
#else
extern  int     interrupt_handler();
#endif

#ifdef MSDOS
extern  int     fpe_handler(void);
#else
extern  int     fpe_handler();
#endif
